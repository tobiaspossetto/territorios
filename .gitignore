node_modules
dist
.env
.DS_Store
*.log

# PoC local de modo admin: registro completo (privado), nunca se sube
public/registro.json

# Capturas generadas localmente (el script sí se versiona)
scripts/salida/
